services:
  myfeeds:
    build: .
    ports:
      - "80:5000"
    volumes:
      - myfeeds-data:/app/data
    environment:
      - FLASK_ENV=production
      - DATABASE_PATH=/app/data/myfeeds.db
    restart: always

volumes:
  myfeeds-data:
